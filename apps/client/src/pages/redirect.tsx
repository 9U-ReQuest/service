export default async function RedirectPage({
  searchParams,
}: { searchParams: Promise<{ code?: string }> }) {
  // TODO: searchParams에 있는 code로
  // 카카오 로그인 API 통신

  return <div>RedirectPage</div>;
}
